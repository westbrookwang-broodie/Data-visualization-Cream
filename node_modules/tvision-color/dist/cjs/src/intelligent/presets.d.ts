import { ColorSpaceArray } from '../type';
declare const presets: {
    [key: string]: ColorSpaceArray;
};
declare const presetsByGroup: {
    [key: string]: ColorSpaceArray[];
};
export { presets, presetsByGroup };
