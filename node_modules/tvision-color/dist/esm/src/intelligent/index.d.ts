import { PaletteSortMethodType } from '../type';
export type IntelligentOption = {
    color?: string;
    colorGamut?: string;
    number?: number;
    sortMethod?: PaletteSortMethodType;
};
export type IntelligentGradationOption = {
    colors: string[];
    step?: number;
    method?: 'darken' | 'range';
};
export type ColorGradationOption = {
    colors: string[];
    step?: number;
    remainInput?: boolean;
};
export default class Intelligent {
    static getRandomPalette(option?: IntelligentOption): string[];
    static getPaletteByGradation(option?: IntelligentGradationOption): string[][];
    static getColorGradations(option?: ColorGradationOption): {
        colors: string[];
        primary: number;
    }[];
    static getNeutralColorGradation(color: string): string[];
    static getNeutralColor(color: string): string[];
}
