export interface IColorGradationParam {
    color: string;
    step: number;
    remainInput: boolean;
}
declare function colorGradation(params: IColorGradationParam): {
    colors: string[];
    primary: number;
};
declare function colorGradation(color: string, step: number): {
    colors: string[];
    primary: number;
};
declare function getNeutralColorGradation(color: string): string[];
declare function getNeutralColor(color: string): string[];
export { colorGradation, getNeutralColorGradation, getNeutralColor };
