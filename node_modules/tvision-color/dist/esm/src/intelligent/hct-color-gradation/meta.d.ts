import { Point } from '../../type';
export declare const hueMatcher: (degree: number) => "Red" | "Orange" | "Yellow" | "Lemon" | "Lime" | "Green" | "Mint" | "Cyan" | "Blue" | "Purple" | "Pink";
export type HueType = ReturnType<typeof hueMatcher>;
export interface HueMetaItem {
    bezier: [number, number, number, number];
    piecewise?: Point[];
    piecewiseBase?: number;
    toneRange: [number, number];
    chromaRange?: [number, number];
}
export declare const HueMeta: {
    [key in HueType]: HueMetaItem;
};
export declare const COLOR_DICT: {
    [key: string]: string;
};
